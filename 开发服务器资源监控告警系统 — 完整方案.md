# 开发服务器资源监控告警系统 — 完整方案

> 版本：1.1 | 目标环境：Linux（生产）/ Windows（开发调试） | 部署方式：单机轻量部署

---

## 一、方案概述

### 1.1 背景与目标

开发一个运行在开发环境服务器上的轻量级监控脚本，定时采集系统资源使用率，当 CPU、内存或磁盘使用率**连续多次**超过阈值时，通过**飞书自定义机器人**（带签名校验）发送告警消息。

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **轻量** | 单 Python 脚本 + 配置文件 + Cron，无需额外服务 |
| **可靠** | 状态持久化，事务级文件锁，发送成功才标记已告警 |
| **安全** | 飞书签名校验，配置文件 600 权限，状态文件 600 权限 |
| **可运维** | 一键部署，日志可查，内置测试模式，便于排查 |
| **跨平台** | Linux 生产部署，Windows 本地开发调试 |

### 1.3 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.6 - 3.13+ | 兼容老服务器，pip 自动解析依赖版本 |
| 系统指标 | `psutil` | 成熟稳定，跨平台 |
| HTTP 客户端 | `requests` | 标准库，简洁 |
| 配置管理 | `PyYAML` | 可读性好，易维护 |
| 定时调度 | Cron | 系统级可靠，无需常驻 |
| 状态存储 | JSON 文件 | 轻量无依赖 |
| 签名算法 | `hmac` + `hashlib` | Python 内置，无额外依赖 |
| 文件锁 | `fcntl.flock` / `msvcrt.locking` | 按平台自动适配 |

---

## 二、核心功能设计

### 2.1 资源采集

| 指标 | 采集方式 | 说明 |
|------|----------|------|
| CPU 使用率 | `psutil.cpu_percent(interval=1)` | 采样 1 秒平均值 |
| 内存使用率 | `psutil.virtual_memory().percent` | 系统整体内存占用 |
| 磁盘使用率 | `psutil.disk_usage(path).percent` | 支持自动发现 + 手动配置 |

### 2.2 磁盘自动发现（v1.1 新增）

生产环境常见 Docker/K8s 服务器，`df` 中大量 **overlay** 挂载显示 **100%**，这是容器层文件系统特性，不代表真实硬盘已满，**不应告警**。

**策略：**

1. `auto_discover: true` 时，通过 `psutil.disk_partitions()` 自动发现真实磁盘挂载
2. 默认排除虚拟/容器文件系统：`overlay`、`aufs`、`tmpfs`、`devtmpfs`、`proc`、`sysfs` 等
3. 默认排除 Docker 相关路径：`/var/lib/docker`、`/overlay2/`、`/merged` 等
4. 按物理设备去重，同一硬盘多个挂载点只监控一次
5. 手动 `paths` 可补充或覆盖特定挂载点的阈值

```
psutil.disk_partitions()
    → 过滤 exclude_fstypes（overlay/tmpfs/...）
    → 过滤 exclude_path_contains（/docker//overlay2/...）
    → 按 device 去重
    → 逐个采集使用率，独立告警
```

**每个挂载点独立判断：**

- 使用率 > 该挂载点 threshold → 连续计数 +1
- 连续达标且未告警 → 发送飞书消息
- 恢复正常 → 计数归零，清除告警标记
- **不需要**所有磁盘都满才告警，也**不会**因 Docker overlay 100% 误报

### 2.3 告警判定规则

- 每个指标**独立维护**连续超标计数和告警状态
- **只有发送成功才标记已告警**，避免网络故障导致告警丢失
- 判定逻辑：

```
指标当前值 > 阈值
    → 连续计数 +1
    → 计数 >= 连续次数(默认3次) 且 未告警
        → 尝试发送告警
        → 发送成功 → 标记已告警
        → 发送失败 → 不标记（下次继续尝试）
指标当前值 <= 阈值
    → 连续计数归零，告警标记清除
```

### 2.4 飞书告警集成

根据飞书官方文档，签名算法实现如下：

```python
def gen_feishu_sign(timestamp: int, secret: str) -> str:
    string_to_sign = f"{timestamp}\n{secret}"
    hmac_code = hmac.new(
        string_to_sign.encode("utf-8"),
        msg=b"",
        digestmod=hashlib.sha256
    ).digest()
    return base64.b64encode(hmac_code).decode("utf-8")
```

### 2.5 跨平台文件锁

| 平台 | 实现 | 注意点 |
|------|------|--------|
| Linux | `fcntl.flock(LOCK_EX)` | 锁整个文件 |
| Windows | `msvcrt.locking` | 加锁/解锁前必须 `seek(0)`，否则 PermissionError |

状态文件采用**原地写入**（truncate + write + fsync），避免 rename 导致锁失效。

### 2.6 错误处理策略

| 异常场景 | 处理方式 |
|----------|----------|
| Python 版本 < 3.6 | 启动时明确报错退出 |
| 配置文件不存在/格式错误 | 打印错误退出（exit 1），不执行采集 |
| 配置项校验失败 | 明确提示缺失项，exit 1 |
| psutil 采集失败 | 记录错误日志，跳过该指标 |
| 状态文件读写失败 | 使用默认状态继续执行 |
| 状态文件损坏 | 自动重置，记录警告日志 |
| 飞书 HTTP 错误（4xx/5xx） | 记录状态码，不标记已告警 |
| 飞书返回非 JSON | 记录错误，不标记已告警 |
| 飞书返回业务错误 | 记录错误码和消息，不标记已告警 |
| 网络超时/不可达 | 重试 3 次（间隔 1 秒），失败则不标记已告警 |
| Windows 控制台编码 | `--test` 输出使用 `[OK]`/`[FAIL]`，避免 GBK emoji 报错 |

---

## 三、配置设计

### 3.1 配置文件 `config.yaml`

```yaml
# ========== 基础配置 ==========
# 日志级别: DEBUG, INFO, WARNING, ERROR
log_level: INFO

# 连续超标次数（达到此次数后触发告警）
consecutive_count: 3

# ========== 监控项配置 ==========
monitors:
  # CPU监控
  cpu:
    enabled: true
    threshold: 90              # 告警阈值（百分比）

  # 内存监控
  memory:
    enabled: true
    threshold: 85

  # 磁盘监控（支持自动发现真实硬盘，排除 Docker overlay 等虚挂载）
  disk:
    enabled: true
    auto_discover: true         # 自动发现所有真实磁盘挂载点
    default_threshold: 90        # 自动发现挂载点的默认告警阈值
    paths: []                    # 可选：手动补充或单独指定阈值
      # - path: /data
      #   threshold: 85
    # exclude_fstypes:           # 可选：额外排除的文件系统类型
    #   - overlay
    # exclude_path_contains:     # 可选：路径包含以下片段则跳过
    #   - /docker/

# ========== 飞书机器人配置（必填） ==========
feishu:
  webhook_url: "https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx"
  secret: "YOUR_SECRET_KEY"
  timeout: 5                    # 请求超时时间（秒），必须 > 0
  max_retries: 3                # 失败重试次数，必须 >= 0
```

**磁盘配置说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `auto_discover` | bool | 是否自动发现真实磁盘挂载，默认 `false`（校验时未配置则 false） |
| `default_threshold` | int | 自动发现挂载点的默认阈值，默认 90 |
| `paths` | list | 手动补充挂载点，可单独设阈值；与自动发现合并 |
| `exclude_fstypes` | list | 额外排除的文件系统类型，默认内置 overlay/tmpfs 等 |
| `exclude_path_contains` | list | 路径包含关键字则跳过，默认内置 docker 相关路径 |

> 校验规则：`auto_discover` 和 `paths` 至少启用其一。

### 3.2 状态文件 `state.json`

```json
{
    "cpu": {
        "count": 2,
        "alerted": false
    },
    "memory": {
        "count": 0,
        "alerted": false
    },
    "disk_/": {
        "count": 1,
        "alerted": false
    },
    "disk_/data": {
        "count": 0,
        "alerted": false
    }
}
```

每个磁盘挂载点使用独立 key（`disk_{挂载路径}`），互不影响。

---

## 四、代码实现

### 4.1 项目结构

**部署包：**

```
monitor-deploy/
├── monitor.py                          # 主监控脚本（约 960 行）
├── config.yaml.example                 # 配置模板
├── requirements.txt                    # Python 依赖
├── deploy.sh                           # 一键部署脚本（Linux）
├── uninstall.sh                        # 卸载脚本（Linux）
├── README.md                           # 使用说明
└── 开发服务器资源监控告警系统 — 完整方案.md
```

**部署后：**

```
/opt/monitor/
├── monitor.py              # 主脚本
├── config.yaml             # 配置文件（权限 600）
├── state.json              # 状态文件（运行时生成，权限 600）
├── requirements.txt        # 依赖清单
└── venv/                   # Python 虚拟环境
```

### 4.2 代码模块说明

> **完整可运行源码见 `monitor.py`**，本文档与代码同步维护，避免双份拷贝漂移。

| 模块 | 函数/类 | 职责 |
|------|---------|------|
| 版本检查 | `MIN_PYTHON` | 启动前校验 Python >= 3.6 |
| 跨平台锁 | `_flock_ex` / `_flock_un` | Linux fcntl / Windows msvcrt |
| 配置 | `load_config` / `validate_config` | 加载并严格校验 YAML |
| 磁盘发现 | `resolve_disk_targets` | 自动发现 + 排除 Docker 虚挂载 |
| 状态 | `StateManager` | 事务级文件锁 + 原地写入 |
| 采集 | `collect_metrics` | CPU / 内存 / 磁盘 |
| 判定 | `evaluate_alerts` | 连续超标计数，独立告警 |
| 飞书 | `gen_feishu_sign` / `send_feishu_alert` | HMAC 签名 + 重试 |
| 入口 | `main` | `--check` / `--test` / 正常监控 |

### 4.3 关键实现片段

**Python 版本检查：**

```python
MIN_PYTHON = (3, 6)
if sys.version_info < MIN_PYTHON:
    sys.stderr.write(
        "ERROR: 需要 Python {maj}.{min} 或更高版本，当前: {ver}\n".format(
            maj=MIN_PYTHON[0], min=MIN_PYTHON[1], ver=sys.version.split()[0],
        )
    )
    sys.exit(1)
```

**磁盘自动发现与过滤：**

```python
def resolve_disk_targets(disk_config):
    # 1. 处理手动 paths
    # 2. auto_discover 时遍历 psutil.disk_partitions()
    # 3. 跳过 overlay/tmpfs 等虚文件系统
    # 4. 跳过 /var/lib/docker、/overlay2/ 等路径
    # 5. 按 device 去重，返回监控目标列表
    ...
```

**`--check` 模式输出磁盘目标：**

```bash
$ python monitor.py --check
配置文件校验通过
磁盘监控目标: 3 个
  - 磁盘(/) [ext4] (阈值 90%)
  - 磁盘(/data) [xfs] (阈值 90%)
  - 磁盘(/backup) [xfs] (阈值 85%)
```

### 4.4 依赖文件 `requirements.txt`

```
# 兼容 Python 3.6 - 3.13+（pip 会按当前 Python 版本自动选择可用包）
psutil>=5.7.0,<7.0.0
requests>=2.20.0,<3.0.0
PyYAML>=5.4,<7.0
```

| Python 版本 | pip 典型解析结果 |
|-------------|------------------|
| 3.6 | requests 2.27.x, PyYAML 5.4+, psutil 5.7+ |
| 3.8+ | requests 2.3x, PyYAML 6.x, psutil 6.x |
| 3.12+ | 同上，均已验证 |

---

## 五、部署方案

### 5.1 部署包结构

```
monitor-deploy/
├── deploy.sh              # 一键部署脚本
├── monitor.py             # 主监控脚本
├── config.yaml.example    # 配置模板
├── requirements.txt       # Python 依赖
├── uninstall.sh           # 卸载脚本
└── README.md              # 说明文档
```

### 5.2 配置模板 `config.yaml.example`

与 §3.1 相同，见项目文件。

### 5.3 一键部署脚本 `deploy.sh`

核心流程：

1. 检查 root 权限
2. 检查 Python 3.6+
3. 复制文件到 `/opt/monitor/`
4. 创建 venv 并安装依赖
5. 运行 `--check` 校验配置
6. 写入 Cron（每 5 分钟）和 logrotate

**Python 版本检查（v1.1）：**

```bash
if ! $PYTHON -c 'import sys; raise SystemExit(0 if sys.version_info >= (3, 6) else 1)'; then
    echo_error "需要 Python 3.6 或更高版本，当前: $PYTHON_VERSION"
    exit 1
fi
```

### 5.4 卸载脚本 `uninstall.sh`

清理内容：

- Cron 任务 `/etc/cron.d/monitor`
- 安装目录 `/opt/monitor`
- 日志轮转 `/etc/logrotate.d/monitor`
- 日志文件 `/var/log/monitor.log`

---

## 六、部署操作流程

### 6.1 飞书机器人配置（一次性）

| 步骤 | 操作 |
|------|------|
| 1 | 在飞书目标群聊中添加「自定义机器人」 |
| 2 | 开启「签名校验」，复制生成的密钥 |
| 3 | 复制 Webhook 地址 |
| 4 | 将密钥和地址填入 `config.yaml` |

### 6.2 服务器端操作（Linux）

```bash
# 1. 上传部署包到服务器
scp -r monitor-deploy/ user@server:/tmp/

# 2. SSH 登录并部署
ssh user@server
cd /tmp/monitor-deploy
sudo ./deploy.sh

# 3. 填写飞书配置（如果部署时未填写）
sudo vi /opt/monitor/config.yaml

# 4. 校验配置并查看磁盘监控目标
cd /opt/monitor
sudo ./venv/bin/python monitor.py --check

# 5. 发送测试消息（可选）
sudo ./venv/bin/python monitor.py --test

# 6. 手动执行一次（可选）
sudo ./venv/bin/python monitor.py

# 7. 验证 Cron 配置
cat /etc/cron.d/monitor
```

### 6.3 本地开发调试（Windows）

```powershell
cd monitor-deploy
pip install -r requirements.txt
copy config.yaml.example config.yaml
# 编辑 config.yaml 填入飞书配置

python monitor.py --check    # 校验配置，列出磁盘监控目标
python monitor.py --test     # 测试飞书连通性
python monitor.py            # 手动执行一次监控
```

---

## 七、运维指南

### 7.1 命令行参数

| 命令 | 说明 |
|------|------|
| `python monitor.py` | 正常执行监控采集与告警 |
| `python monitor.py --check` | 校验配置文件，并列出磁盘监控目标 |
| `python monitor.py --test` | 发送测试消息到飞书 |

### 7.2 日志查看

```bash
# 实时查看日志
tail -f /var/log/monitor.log

# 查看最近错误
grep "ERROR" /var/log/monitor.log | tail -20

# 查看告警记录
grep "尝试发送告警" /var/log/monitor.log

# 查看恢复记录
grep "已恢复正常" /var/log/monitor.log

# 查看跳过的 Docker 虚挂载（DEBUG 级别）
grep "跳过挂载" /var/log/monitor.log
```

### 7.3 状态文件管理

```bash
# 查看当前状态
cat /opt/monitor/state.json

# 手动重置所有状态（下次从零开始计数）
rm -f /opt/monitor/state.json
```

### 7.4 修改监控频率

```bash
sudo vi /etc/cron.d/monitor

# 例如改为每 2 分钟：
# */2 * * * * root cd /opt/monitor && /opt/monitor/venv/bin/python monitor.py >> /var/log/monitor.log 2>&1
```

### 7.5 常见问题排查

| 问题现象 | 排查方法 |
|----------|----------|
| 脚本不执行 | `grep CRON /var/log/syslog \| grep monitor` |
| 配置文件错误 | `python monitor.py --check` |
| 飞书收不到消息 | `python monitor.py --test` |
| 飞书签名失败 | 检查 `feishu.secret` 是否与飞书控制台一致 |
| Docker overlay 误报 | 确认 `auto_discover: true`，运行 `--check` 查看目标列表 |
| 漏监控某个盘 | `--check` 查看目标，必要时在 `paths` 手动补充 |
| Python 依赖缺失 | `./venv/bin/pip list` |
| 权限问题 | `ls -la /opt/monitor/` |
| 状态文件异常 | `cat state.json`，必要时删除重置 |

---

## 八、方案评审要点

| 维度 | 评审点 | 状态 |
|------|--------|------|
| **正确性** | 阈值判断逻辑 | ✅ 连续超标计数，防抖动 |
| **正确性** | 告警状态更新时机 | ✅ 发送成功才标记 |
| **正确性** | 磁盘独立告警 | ✅ 每个挂载点独立计数/阈值 |
| **正确性** | Docker 虚挂载过滤 | ✅ 排除 overlay/tmpfs 等 |
| **可靠性** | 状态持久化 | ✅ JSON 文件，原地原子写入 |
| **可靠性** | 并发控制 | ✅ 跨平台事务级文件锁 |
| **可靠性** | 告警重试 | ✅ 3 次重试，间隔 1 秒 |
| **兼容性** | Python 版本 | ✅ 3.6 - 3.13+ |
| **兼容性** | 跨平台 | ✅ Linux 生产 + Windows 调试 |
| **安全性** | 飞书签名 | ✅ 按官方文档实现 |
| **安全性** | 密钥存储 | ⚠️ 明文，依赖文件权限(600)保护 |
| **可运维性** | 配置校验 | ✅ 启动时完整校验 |
| **可运维性** | 测试模式 | ✅ --test / --check（含磁盘目标预览） |
| **可运维性** | 部署/卸载 | ✅ 一键脚本，部署时自动校验 |
| **扩展性** | 多磁盘 | ✅ 自动发现 + 手动补充 |
| **扩展性** | 分指标阈值 | ✅ 每个指标/挂载点独立阈值 |
| **性能** | 单次执行 | < 3 秒（含 CPU 1 秒采样） |
| **依赖** | 外部依赖 | 3 个库，均有官方支持 |

---

## 九、交付清单

- [x] 完整代码（约 960 行，见 `monitor.py`）
- [x] 配置模板（含磁盘自动发现）
- [x] 依赖清单（Python 3.6 - 3.13+ 兼容）
- [x] 一键部署脚本（含 Python 版本校验）
- [x] 卸载脚本
- [x] 飞书签名集成（符合官方规范）
- [x] `--check` / `--test` 模式
- [x] `--check` 预览磁盘监控目标
- [x] 跨平台事务级文件锁（Linux fcntl / Windows msvcrt）
- [x] 原地原子写入（truncate + write + fsync）
- [x] 磁盘自动发现，排除 Docker overlay 虚挂载
- [x] 严格配置校验
- [x] HTTP 响应分阶段错误处理
- [x] 发送成功才标记已告警
- [x] Windows 本地开发调试支持

---

## 十、版本变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | - | 初始方案：CPU/内存/磁盘监控，飞书告警，Linux 部署 |
| 1.1 | 2026-07 | 磁盘自动发现；排除 Docker overlay；跨平台文件锁；Python 3.6-3.13+ 兼容；`--check` 预览磁盘目标；Windows 调试支持 |

---

*方案结束*
